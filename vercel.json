{
   "version": 2,
   "name": "api-portfolio-backend",
   "builds": [
      { 
         "src": "server.js",
         "use": "@now/node"
      }
   ],
   "routes": [
      { 
         "src": "/(.*)",
         "dest": "server.js" }
   ]
}
